# Projeto Workshop GitHub - COTI Informática

Este repositório contém um projeto front-end (HTML, CSS e JavaScript) desenvolvido durante o workshop do GitHub realizado na COTI Informática.

## Sobre o projeto

O projeto foi criado como exercício prático durante o workshop para aprender sobre controle de versão com Git/GitHub e a construção de páginas web estáticas. Ele reúne arquivos HTML como páginas, CSS para estilos e JavaScript para comportamento (se houver scripts no repositório).

## Tecnologias usadas

- HTML — marcação das páginas
- CSS — estilos e layout
- JavaScript — interatividade e comportamento (quando aplicável)

> Observação: A análise de linguagem do repositório indica que a maior parte do código é HTML.

## Estrutura sugerida do repositório

- index.html — página principal
- /css — arquivos de estilo (ex.: styles.css)
- /js — scripts JavaScript (ex.: app.js)
- /assets — imagens e outros recursos

A estrutura exata pode variar dependendo dos arquivos adicionados durante o workshop.

## Como abrir e rodar o projeto no Visual Studio Code (VSCode)

1. Pré-requisitos
   - Instale o Visual Studio Code: https://code.visualstudio.com/
   - (Opcional, recomendado) Instale a extensão Live Server (por Ritwick Dey) no VSCode para recarregamento automático da página.

2. Clonar o repositório (opcional se já tiver o código local)
   - Pelo terminal:
     ```bash
     git clone https://github.com/mmagdev/projeto-github.git
     cd projeto-github
     ```
   - Ou baixe o ZIP no GitHub e extraia a pasta localmente.

3. Abrir o projeto no VSCode
   - No VSCode, escolha "File » Open Folder..." (ou "Abrir pasta...") e selecione a pasta do projeto.
   - Alternativamente, do terminal dentro da pasta do projeto execute `code .` para abrir o VSCode nessa pasta.

4. Abrir e visualizar a página
   - No painel do Explorador do VSCode, clique em `index.html` para abrir o arquivo.
   - Com a extensão Live Server instalada: clique com o botão direito em `index.html` e selecione "Open with Live Server". O navegador será aberto mostrando a página e atualizará automaticamente sempre que você salvar alterações.
   - Sem Live Server: abra `index.html` diretamente no navegador (arrastando o arquivo ou usando "Open File"). Note que alguns recursos (como requisições AJAX locais) podem ser bloqueados quando aberto via protocolo `file://`; nesse caso, use Live Server ou um servidor local.

5. Depuração
   - Use as Ferramentas de Desenvolvedor do navegador (geralmente F12) para inspecionar o DOM, verificar estilos e ver mensagens do console JavaScript.

6. Testes rápidos
   - Faça alterações em arquivos `.html`, `.css` ou `.js` e salve; com Live Server elas serão refletidas no navegador imediatamente.

## Contribuindo

Este é um projeto de aprendizado. Você pode contribuir das seguintes formas:

1. Faça um fork do repositório.
2. Crie uma branch com sua alteração: `git checkout -b minha-melhora`.
3. Faça commits claros e descriptivos.
4. Abra um Pull Request descrevendo suas mudanças.

## Dúvidas / Contato

Se você participou do workshop, consulte o material e os instrutores para dúvidas específicas. Para sugestões ou problemas neste repositório, abra uma Issue no GitHub.

---

README gerado/atualizado para o repositório "projeto-github" — projeto do workshop COTI Informática.

---

# Projeto Workshop GitHub - COTI Informática (English)

This repository contains a front-end project (HTML, CSS and JavaScript) developed during the GitHub workshop held at COTI Informática.

## About the project

The project was created as a hands-on exercise during the workshop to learn about version control with Git/GitHub and building static web pages. It includes HTML files for pages, CSS for styling, and JavaScript for behavior (if there are scripts in the repository).

## Technologies used

- HTML — page markup
- CSS — styles and layout
- JavaScript — interactivity and behavior (when applicable)

> Note: The repository language analysis indicates that most of the code is HTML.

## Suggested repository structure

- index.html — main page
- /css — style files (e.g., styles.css)
- /js — JavaScript scripts (e.g., app.js)
- /assets — images and other resources

The exact structure may vary depending on the files added during the workshop.

## How to open and run the project in Visual Studio Code (VSCode)

1. Prerequisites
   - Install Visual Studio Code: https://code.visualstudio.com/
   - (Optional, recommended) Install the Live Server extension (by Ritwick Dey) in VSCode for automatic page reload.

2. Clone the repository (optional if you already have the code locally)
   - From the terminal:
     ```bash
     git clone https://github.com/mmagdev/projeto-github.git
     cd projeto-github
     ```
   - Or download the ZIP from GitHub and extract the folder locally.

3. Open the project in VSCode
   - In VSCode, choose "File » Open Folder..." and select the project folder.
   - Alternatively, from the terminal inside the project folder run `code .` to open VSCode in that folder.

4. Open and view the page
   - In the VSCode Explorer panel, click `index.html` to open the file.
   - With Live Server installed: right-click `index.html` and choose "Open with Live Server". Your browser will open the page and automatically refresh when you save changes.
   - Without Live Server: open `index.html` directly in the browser (drag the file or use "Open File"). Note that some features (like local AJAX requests) may be blocked when opened via the `file://` protocol; in that case, use Live Server or a local server.

5. Debugging
   - Use the browser Developer Tools (usually F12) to inspect the DOM, check styles, and view JavaScript console messages.

6. Quick testing
   - Edit `.html`, `.css`, or `.js` files and save; with Live Server, changes will be reflected in the browser immediately.

## Contributing

This is a learning project. You can contribute in the following ways:

1. Fork the repository.
2. Create a branch for your change: `git checkout -b my-improvement`.
3. Make clear, descriptive commits.
4. Open a Pull Request describing your changes.

## Questions / Contact

If you attended the workshop, check the workshop materials and instructors for specific questions. For suggestions or issues in this repository, open an Issue on GitHub.

---

README generated/updated for the "projeto-github" repository — workshop project from COTI Informática.
